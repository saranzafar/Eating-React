const conf = {
    // databaseUrl: String(import.meta.env.URL || "http://localhost:8000/api/v1/")
    databaseUrl: String(import.meta.env.URL || "https://survey-app-backend-liard.vercel.app/api/v1/")
}

export default conf
